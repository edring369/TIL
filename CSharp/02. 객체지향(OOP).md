# 객체지향(OOP)

## 목차
- [0. 객체지향 개요](#0-객체지향-개요)
- [1. 클래스](#1-클래스)
  - [1.1 클래스란?](#11-클래스란)
  - [1.2 생성자](#12-생성자)
  - [1.3 this](#13-this)
  - [1.4 Property](#14-property)
  - [1.5 static](#15-static)
- [2. 상속과 다형성](#2-상속과-다형성)
  - [2.1 상속](#21-상속)
  - [2.2 object](#22-object)
  - [2.3 virtual](#23-virtual)
  - [2.4 override](#24-override)
  - [2.5 abstract](#25-abstract)
  - [2.6 interface](#26-interface)
- [3. enum](#3-enum)
- [4. struct](#4-struct)

## 0. 객체지향 개요
객체지향은 코드 내의 모든 것을 객체로 표현하려는 프로그래밍 패러다임이다. 기계 관점(절차지향)에서 사람관점(객체지향)으로 변화한 것으로 `class`라는 문법을 통해 표현한다.

- 객체지향 언어의 3가지 특성
  - 캡슐화: 코드와 코드의 데이터 결합하여, 정보를 은닉한다.
  - 상속성: 한 객체가 다른 객체의 특성을 획득한다.
  - 다형성: 하나의 Cs인터페이스(interface)를 통해 일반적인 클래스의 행위들을 접근할 수 있도록 한다.
## 1. 클래스
### 1.1 클래스란?
클래스(class)는 객체를 만들기 위한 청사진, 틀을 제공하는 **자료형**이다. 
`new`로 인스턴스 객체를 생성하여 사용한다. 인스턴트 객체란 클래스에 의해 생성된 실체로 메모리에 생성된 **값**이다.

접근 제한자로 캡슐화(은닉성)를 구현한다.
- `private`: 클래스의 내부에서만 접근 가능, 파생 클래스도 접근 불가
- `public`: 클래스의 내/외부 모두 접근 가능
- `protected`: 파생 클래스에서만 접근 가능

예제) 클래스 객체 생성
```cs
using System;

class Program
{
    static void Main(string[] args)
    {
        Cat cat = new Cat(); //인스턴스: cat, C#에서는 new로 인스턴스 객체 생성
        cat.name = "Kitty"; 
        cat.color = "White";
        cat.Meow();
    }
}

class Cat
{
    public string name;  //멤버 변수(필드)
    public string color; //멤버 함수(메서드)
    public void Meow() { Console.WriteLine("야옹"); }
}
```

### 1.2 생성자
생성자는 객체의 삶과 죽음이다.
- 생성자는 클래스 이름과 같다.
- 생성자는 객체 멤버의 값을 초기화 한다.

예제) 생성자 예제 
```cs
using System;

class Program
{

    static void Main(string[] args)
    {
        Cat cat = new Cat() { name = "Yaong" };
        Console.WriteLine(cat.name); // Yaong 출력
    }
}
internal class Cat
{
    public string name;  //멤버 변수(필드)
    public string color; //멤버 함수(메서드

    public Cat() // 생성자는 외부에서 생성하려고 만드는 것이니 public으로 선언
    {
        name = "Kitty";
        color = "White";
    }
    public void Meow() { Console.WriteLine("야옹"); }
}
```
### 1.3 this

### 1.4 Property

### 1.5 static

## 2. 상속과 다형성

### 2.1 상속

### 2.2 object

### 2.3 virtual

### 2.4 override

### 2.5 abstract

### 2.6 interface

## 3. enum

## 4. struct
